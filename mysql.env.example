# Example environment variables for MySQL with multiple databases
# Copy this file to .env in the same directory as mysql.yml to use these settings

# Create additional databases (comma-separated list)
# The primary database 'my_db' will still be created by default
MYSQL_MULTIPLE_DATABASES=app1_db,app2_db,test_db,analytics_db

# You can also override other MySQL settings if needed:
# MYSQL_ROOT_PASSWORD=your_secure_root_password
# MYSQL_PASSWORD=your_secure_user_password
# MYSQL_USER=your_username
# MYSQL_DATABASE=your_primary_db